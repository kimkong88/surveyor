export default function CapturePage() {
    return (
        <section aria-labelledby="capture-title" className="p-4">
            <h1 id="capture-title">Capture</h1>
            <p>Camera or file upload UI will be placed here.</p>
        </section>
    );
}
